/* Bubble Sort
 * Kaitlin Berryman */
 #include <stdio.h>
 
 void bubbleSort(int numbers[], int n)
 {
	for(int a=0; a<n-1; a++)
		for(int b=0; b<n-1-a; b++)
			if(numbers[b] > numbers[b+1]
			{
				int temp = numbers[b];
				numbers[b] = numbers[b+1];
				numbers[b+1] = temp;
			}
		}
	}
 }
 
 int main()
 {
	 
 }
